Usage: aeiou [OPTIONS...] [FILE]...

Replace unicode sequences by UTF-8 characters, and vice versa.

Code sequences can be either `\uxxxx` for 16-bit Unicode or
\Uxxxxxxxx for 21-bit Unicode.

  -c, --encode          Replace unicode characters by code sequences.
  -d, --decode          Replace code sequences by unicode characters.
